# Python
__pycache__/
*.py[cod]
.env
venv/
.venv/
# VITE_API_BASE_URL bukan rahasia (dibaca browser di bundle), API butuh ini saat build
!web/.env

# ML data (large files)
ml/data/processed/*
ml/data/raw/*
!ml/data/processed/.gitkeep
!ml/data/raw/.gitkeep
!ml/data/raw/TLKM.JK.csv

# ML models (large binary) — .pkl (scaler) TIDAK diignore, API butuh itu saat runtime
ml/models/**/*.h5
ml/models/**/*.keras

# Node
web/node_modules/
web/.next/

# OS
.DS_Store
Thumbs.db


